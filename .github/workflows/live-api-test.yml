name: Live cloud product test

on:
  push:
    paths:
      - 'worker/src/index.js'
      - 'worker/src/confidence.js'
      - 'tests/confidence.test.mjs'
      - 'tests/live-api.test.mjs'
      - '.github/workflows/live-api-test.yml'
  workflow_dispatch:

jobs:
  live-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Test confidence v2
        run: node tests/confidence.test.mjs
      - name: Wait for Cloudflare deploy and test 3 cloud products
        run: node tests/live-api.test.mjs
