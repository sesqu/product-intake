{
  "name": "product-intake-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "cloudflare": {
    "bindings": {
      "ALLEGRO_CLIENT_SECRET": {
        "description": "Client Secret z aplikacji Allegro. Wklej go wyłącznie do Cloudflare jako sekret — nigdy do GitHuba."
      },
      "AUTH": {
        "description": "KV do przechowywania tokenów OAuth Allegro. Cloudflare utworzy zasób automatycznie."
      }
    }
  }
}